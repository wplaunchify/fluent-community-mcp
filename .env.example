# FluentCommunity MCP Server Configuration

# WordPress Site URL (without trailing slash)
WP_SITE_URL=https://your-site.com

# WordPress REST API Authentication
# Option 1: Application Password (Recommended)
WP_USERNAME=your-username
WP_APP_PASSWORD=your-application-password

# Option 2: JWT Token (if using JWT authentication plugin)
# WP_JWT_TOKEN=your-jwt-token

# FluentCommunity Database Table Prefix (usually wp_)
WP_DB_PREFIX=wp_

# FluentCommunity Table Prefix (usually fcom_)
FC_TABLE_PREFIX=fcom_

# MCP Server Settings
MCP_SERVER_NAME=fluent-community-manager
MCP_SERVER_VERSION=1.0.0
